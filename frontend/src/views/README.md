# View Structure

프론트 화면은 역할 기준으로 아래 구조를 사용합니다.

- `views/admin`: 관리자 화면
- `views/auth`: 로그인, 회원가입, 비밀번호 재설정, 소셜 로그인
- `views/user`: 일반 사용자 화면

현재 라우터는 이 새 구조만 참조합니다.

남아 있는 `views` 루트의 예전 파일들은 에디터 또는 개발 서버가 파일을 점유하고 있어 삭제가 보류된 상태입니다.
실행에는 사용되지 않으며, 잠금이 풀리면 정리 대상입니다.